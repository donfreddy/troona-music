import 'package:isar_plus/isar_plus.dart';
import 'package:troona/features/library/domain/entities/playlist.dart';

// Generated by `dart run build_runner build --delete-conflicting-outputs`.
part 'playlist_model.g.dart';

/// Isar-persisted user-created playlist.
///
/// [playlistId] is a stable string key distinct from the Isar auto-increment
/// [id], allowing playlists to be referenced across migrations.
@collection
class PlaylistModel {
  /// Isar auto-increment primary key. Null until the record is first written.
  Id? id;

  /// Application-level stable identifier (e.g. UUID).
  @Index(unique: true)
  late String playlistId;

  late String name;

  /// Optional path to a cached mosaic/cover artwork JPEG.
  String? artworkPath;

  /// Converts this model to the immutable domain [Playlist] entity.
  Playlist toEntity() =>
      Playlist(id: playlistId, name: name, artworkPath: artworkPath);
}
